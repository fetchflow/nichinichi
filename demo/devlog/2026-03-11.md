# 2026-03-11

---
09:00 | sprint kickoff — permissions system is the main track this week @acme #log
---
10:30 | decided to model permissions as resource:action pairs, not role bitmasks @acme #decision

       Bitmasks are fast but opaque — hard to audit, hard to explain to stakeholders.
       String pairs like "billing:read" are self-documenting.
       Performance difference is negligible at our scale.
---
13:00 | fixed a bug in the config loader — tilde expansion was breaking on Windows paths @personal #solution

       The bug: using string replace on "~" without checking platform separators.
       Fix: use the dirs crate's home_dir() function and join the remainder.
---
16:45 | realised that most of my best work this year was on infrastructure, not features #reflection

       Looking at the log: auth middleware, CI fixes, DB optimizations.
       That's the staff eng track — enablement over pure delivery.
---
