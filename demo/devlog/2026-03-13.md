# 2026-03-13

---
09:20 | pairing with David on the org scoping middleware @acme #log
---
11:45 | fixed the N+1 query in the goal steps endpoint @acme #solution

       Root cause: loading steps one-by-one inside a loop instead of a JOIN.
       Fix: single query with LEFT JOIN goal_steps and grouping in application layer.
       Reduced avg response time from 180ms to 12ms on 50 steps.
---
14:30 | working on personal devlog app features @personal #log
---
16:00 | shipped the org switcher UI — live on staging @personal #score

       Took longer than expected due to the persistence layer needing a settings table.
       But now active_org is persisted across sessions, which is the right behavior.
---
