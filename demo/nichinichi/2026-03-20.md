# 2026-03-20

---
09:10 | standup @acme #log

       Yesterday: billing page live on staging, auth fix merged.
       Today: background job bug, then rate limiter design doc.
       Blockers: none.
---
10:30 | resolved a race condition in the background job processor @acme #solution

       Root cause: multiple workers acquiring the same job lease due to a
       non-atomic check-then-lock pattern. Fixed by using SELECT ... FOR UPDATE
       with a single transaction to atomically claim the job.
       Tested with 10 concurrent workers — no duplicate execution.
---
14:00 | closed 3 issues, unblocked the mobile team on the permissions endpoint @acme #score

       - #412: background job race condition (fixed today)
       - #398: missing org field in GET /users response
       - #401: rate limit headers not returned on 429 responses
       Raj can now unblock the iOS client refresh flow.
---
16:45 | finally understood how FTS5 content tables and triggers work #reflection

       Spent an hour reading SQLite docs. The trick: FTS5 with content='entries'
       is NOT automatically kept in sync — you need explicit INSERT/DELETE/UPDATE
       triggers on the base table. The 'delete' command in FTS5 is unintuitive.
       Worth documenting this in the codebase.
---
