# 2026-03-18

---
09:15 | standup: auth done, moving to billing integration @acme #log
---
10:45 | decided to use Redis for session cache instead of Postgres @acme #decision

       Options considered:
       - Postgres: consistent, already in stack, slower for ephemeral data
       - Redis: TTL-native, sub-millisecond reads, operationally simple for cache
       Picked Redis. The TTL support alone makes it the right call for tokens.
       Rejected Postgres because session data doesn't need ACID guarantees.
---
13:00 | reviewed David's PR — billing UI, looks solid, left 2 comments @acme #log
---
15:20 | the flaky integration test in CI was a race condition on the DB fixture setup @acme

       Fixed by adding explicit wait for connection pool readiness before seeding.
       Took longer than expected — test infrastructure is always a rabbit hole.
---
17:00 | shipped billing page to staging @personal #score

       First time shipping something entirely solo end to end in a while.
       Felt good. Stripe webhook integration worked first try.
---
